<div class="card-container">
  <div class="card">
    <turbo-frame id="<%= dom_id @offer %>">
      <h2 class="title m-4">Update Offer</h2>
      <%= render "form", offer: @offer %>
    </turbo-frame>
  </div>
</div>
